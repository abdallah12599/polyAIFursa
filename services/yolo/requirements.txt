# FastAPI and Uvicorn (for web API)
fastapi>=0.115.0
uvicorn>=0.21.1
httpx>=0.28.1
python-dotenv>=1.0.0

# Pillow for image handling
pillow>=9.5.0

# Use headless OpenCV to avoid missing display/xcb libs on slim images
opencv-python-headless>=4.7.0

# Ultralytics YOLOv8 (includes minimal dependencies)
ultralytics>=8.0.0
python-multipart>=0.0.6

# Metrics
prometheus-fastapi-instrumentator>=6.0.0

# Database (SQLAlchemy ORM; psycopg2-binary for PostgreSQL in production)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# AWS SDK (S3 as the intermediary image store between agent and yolo)
boto3>=1.34.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
