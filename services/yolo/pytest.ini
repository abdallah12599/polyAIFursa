[pytest]
# Make the `app` module importable from inside tests/.
pythonpath = .
# Generate coverage for the app module, including an XML report for Codecov.
addopts = --cov=app --cov-report=term-missing --cov-report=xml
